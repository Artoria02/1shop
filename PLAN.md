# 多商家电商平台完整商业版规划

## Summary
建设一个面向中国大陆市场的多商家实物电商平台，技术栈采用 `Next.js + TypeScript + PostgreSQL`。平台模式为商家入驻、平台统一收款、订单完成后按周期给商家结算。首版包含买家端、商家后台、平台管理后台、交易闭环、基础营销、售后、结算和运营能力。

默认实现形态：
- 前端：Next.js App Router，买家端、商家端、平台后台共用一个应用。
- 后端：Next.js Server Actions/API Routes，业务逻辑分层封装为 service。
- 数据库：PostgreSQL + Prisma。
- 缓存/队列：Redis，用于购物车、验证码、库存锁、异步任务。
- 文件存储：对象存储，用于商品图、资质文件、售后凭证。
- 支付：微信支付 + 支付宝，平台统一收款。
- 登录：买家端（手机号+验证码/账号密码）、商家端（账号密码，查 MerchantStaff）、管理端（邮箱密码，独立 PlatformAdmin 表）。三端密码隔离，session cookie 仅存 userId+end+roles，昵称头像从对应身份表实时查询。

## Key Changes
核心业务模块：
- 用户体系：买家、商家员工共享 `User` 认证表，通过 `BuyerProfile` / `MerchantStaff` 身份详情表区分端；平台管理员独立 `PlatformAdmin` 表。三端密码隔离（买家密码→BuyerProfile、商家密码→User、管理密码→PlatformAdmin）。采用 RBAC 权限控制。
- 商家入驻：商家提交企业/个体资质、联系人、结算账户，平台人工审核后开店。
- 商品系统：SPU/SKU、类目、品牌、规格、价格、库存、上下架、商品审核。
- 交易系统：购物车、订单、支付、发货、确认收货、取消、退款、退货退款。
- 营销系统：优惠券、满减、限时折扣、包邮规则，支持平台级和店铺级活动。
- 物流系统：首版由商家手填快递公司和运单号，预留物流轨迹 API 接入位。
- 结算系统：订单按商家拆单，平台扣佣后生成结算单，支持周期结算和人工打款确认。
- 评价系统：买家确认收货后评价商品和店铺，平台可审核/隐藏违规评价。
- 后台运营：商家审核、商品审核、订单监管、退款仲裁、佣金配置、首页配置、数据报表。

主要数据模型：
- `User`：手机号、邮箱、密码哈希（商家端）、头像、状态、注册来源。无 kind 字段。
- `BuyerProfile`：买家身份详情（密码哈希、显示昵称），与 User 1:1。
- `PlatformAdmin`：平台管理员独立认证表（邮箱、密码哈希、昵称、头像），不与 User 共享。
- `Role` / `Permission`：平台、商家后台权限。
- `Merchant`：商家主体、店铺信息、审核状态、佣金比例。
- `MerchantStaff`：商家员工关联（显示昵称、是否店主），与 User 1:N。
- `Category` / `Brand`：平台统一类目和品牌。
- `Product` / `Sku`：商品基础信息、规格、价格、库存、审核状态。
- `CartItem`：买家购物车。
- `Order` / `OrderItem`：主订单、按商家拆分的子订单、商品明细。
- `Payment`：支付渠道、支付单号、回调状态、金额。
- `Shipment`：物流公司、运单号、发货时间。
- `Refund`：仅退款、退货退款、状态流转、凭证。
- `Promotion` / `Coupon`：优惠规则和用户领券记录。
- `Settlement` / `SettlementItem`：商家结算单、佣金、应结金额、打款状态。
- `Review`：评分、内容、图片、审核状态。
- `AuditLog`：关键后台操作日志。

关键接口边界：
- 买家端 API：注册登录、商品列表/详情、购物车、下单、支付、订单、售后、评价。
- 商家端 API：店铺资料、商品管理、订单发货、退款处理、营销配置、结算查看。
- 平台后台 API：商家审核、商品审核、订单监管、退款仲裁、佣金配置、结算确认、报表。
- 支付回调 API：微信/支付宝异步通知必须幂等处理，支付状态只允许单向推进。
- 权限中间件：所有后台接口必须校验身份、角色、商家归属和资源归属。

关键业务规则：
- 一个买家订单可包含多个商家商品，提交后按商家拆分子订单。
- 库存在下单时锁定，未支付超时自动释放。
- 支付成功后订单进入待发货。
- 商家只能操作自己店铺的商品、订单、退款和结算数据。
- 平台管理员可介入退款争议并覆盖商家处理结果。
- 结算只统计已完成且超过售后保护期的订单。
- 所有金额使用整数分存储，禁止浮点金额计算。
- 所有支付、退款、库存、结算操作必须有幂等键和操作日志。

## Implementation Plan
第一阶段：项目基础
- 初始化 Next.js + TypeScript + Prisma + PostgreSQL + Redis。
- 建立统一代码结构：`app` 页面路由、`server` 业务服务、`db` 数据访问、`components` UI、`lib` 工具。
- 接入认证、RBAC、错误处理、请求日志、审计日志。
- 建立基础后台布局：买家端、商家后台、平台后台三套导航。

第二阶段：商家与商品
- 实现商家入驻申请、资质上传、平台审核、店铺启用/禁用。
- 实现类目、品牌、规格、SPU/SKU、商品图、库存、上下架。
- 商品发布后进入平台审核，审核通过后买家端可见。
- 商家后台只展示和操作自身店铺数据。

第三阶段：交易闭环
- 实现商品浏览、搜索筛选、购物车、提交订单。
- 实现订单拆单、库存锁定、支付单生成、支付回调、超时取消。
- 实现商家发货、买家确认收货、订单完成。
- 实现订单状态机，禁止非法状态跳转。

第四阶段：售后、营销、结算
- 实现仅退款、退货退款、商家处理、平台仲裁。
- 实现优惠券、满减、限时折扣、包邮规则。
- 实现佣金配置、结算周期、结算单生成、人工打款确认。
- 实现评价、评分、平台审核和违规隐藏。

第五阶段：运营与商业化完善
- 实现平台首页配置、推荐位、搜索排序基础规则。
- 实现平台数据看板：GMV、订单数、退款率、商家销售排行、商品排行。
- 实现商家数据看板：销售额、订单趋势、售后率、商品表现。
- 补齐安全策略：接口限流、短信限流、后台敏感操作二次确认、导出权限控制。

## Test Plan
单元测试：
- 金额计算、优惠叠加、佣金计算、库存扣减、订单状态机。
- 权限判断、商家资源归属判断、支付回调幂等处理。

集成测试：
- 商家入驻到审核通过。
- 商品发布到审核上架。
- 买家下单、支付成功、商家发货、买家确认收货。
- 未支付订单超时释放库存。
- 多商家购物车提交后正确拆单。
- 退款申请、商家同意、平台仲裁。
- 订单完成后进入可结算范围并生成结算单。

端到端测试：
- 买家完整购物流程。
- 商家完整履约流程。
- 平台管理员审核、监管、结算流程。
- 权限隔离：商家 A 不能访问商家 B 的订单、商品、结算。

验收标准：
- 买家可完成真实交易闭环。
- 商家可独立管理商品、订单、售后和结算。
- 平台可审核商家/商品、监管交易、处理争议、配置佣金。
- 支付、库存、订单、退款、结算具备幂等和审计能力。
- 核心流程有自动化测试覆盖。

## Assumptions
- 首版只做中国大陆市场，默认中文、人民币、手机号登录。
- 首版只支持实物商品。
- 首版物流由商家手填快递公司和运单号，不接自动轨迹查询。
- 首版采用平台统一收款、周期结算给商家的模式。
- 商家入驻采用人工审核。
- 营销首版只做优惠券、满减、限时折扣和包邮。
- 法务、支付资质、税务、ICP备案、短信签名、微信/支付宝商户号由业务方准备。
- 当前仓库结构尚未确认；实施前需要先读取项目文件并把该规划映射到实际目录和已有技术栈。
